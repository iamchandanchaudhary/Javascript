    // Window Object
    console.log(window);

    // document Object --> Show HTML program
    console.log(document);

    // Access all Objects
    console.dir(document);

    // document Object --> Show HTML tags
    console.dir(document.all);